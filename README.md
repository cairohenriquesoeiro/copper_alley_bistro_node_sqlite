# Copper Alley Bistro

Full-stack restaurant site with Node.js + Express + SQLite.

## Setup
```bash
cd backend
npm install
npm start
```
Open http://localhost:3000

API:
- GET /api/bookings
- POST /api/bookings
- DELETE /api/bookings/:id

Frontend is served statically from ../frontend
